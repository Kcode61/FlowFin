# Frontend FlowFin

Este é o frontend em Next.js para o FlowFin.

Use a pasta `app` para as páginas e `components` para os blocos da interface.

Para rodar localmente:

```bash
npm install
npm run dev
```

Abra `http://localhost:3000` no navegador.
